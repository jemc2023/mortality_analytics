services:
  web-server:
    image: nginx:alpine
    container_name: nginx_semis2
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ../../data/raw/who_mortality:/usr/share/nginx/html:ro
    ports:
      - 8000:8000
